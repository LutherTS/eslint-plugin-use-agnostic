// "use server functions"

const x = 1;
export const y = 2;
export const z = 3;

export default x;

/* Notes
Place the file in the directive21/imported/valid folder, use console.debug, and see the directive21 results while running `npm run test`.
Use a same-level folder called "not-valid" to house the actual files from the "valid" folder in the meantime. 
*/
